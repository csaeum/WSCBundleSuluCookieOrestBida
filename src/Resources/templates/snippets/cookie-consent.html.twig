{#
    WSC Cookie Consent Snippet
    This snippet renders the Orest Bida Cookie Consent banner with configuration from Sulu Admin

    Usage: Include this snippet area in your base template:
    {% include sulu_snippet_load_by_area('cookie-consent') %}
#}
{% if content.enabled|default(true) %}
    {{ wsc_cookie_consent_render(content)|raw }}
{% endif %}
